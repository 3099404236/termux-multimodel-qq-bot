# 项目架构

## 数据流

1. NapCat 从 QQ 收到群消息，通过 OneBot 连接交给 AstrBot。
2. AstrBot 完成唤醒检查、群聊记忆注入、工具定义和模型请求组装。
3. OpenAI 兼容提供商把请求发到 `127.0.0.1:8791`。
4. 路由桥读取最新用户消息：默认进入 Antigravity；明确指定 Opus 时切换到配置的 Opus 模型；明确指定 GPT 时转发到 `127.0.0.1:8766`。
5. 配置 Manager 时，Antigravity 路由原样转发 OpenAI 消息和工具定义；未配置时复用持久化 PTY，并把严格 YAML 信封解析成最终文本或工具调用。ChatGPT 路由通过 CDP 操作已登录网页，并返回相同的 OpenAI 风格响应。
6. AstrBot 执行工具调用或整理最终回答，将超长文本拆成安全大小的合并转发节点，再由 NapCat 发回 QQ。

## 组件与端口

| 组件 | 默认地址 | 作用 |
| --- | --- | --- |
| NapCat WebUI | `127.0.0.1:6099` | QQ 登录与 OneBot 配置 |
| AstrBot WebUI | `127.0.0.1:6185` | 提供商、人格、插件与日志管理 |
| Antigravity 路由桥 | `127.0.0.1:8791` | OpenAI 兼容入口、模型路由、PTY 管理 |
| Antigravity Manager | 部署方决定 | 推荐的远程 OpenAI 兼容后端；凭据仅保存在本地私有环境文件 |
| 单模型受限网关 | `127.0.0.1:18046` | 独立分享 Key、模型强制改写、管理路由隔离；公网使用必须置于 HTTPS 后面 |
| ChatGPT 网页桥 | `127.0.0.1:8766` | OpenAI 兼容的网页会话代理 |
| Chrome CDP | `127.0.0.1:9222` | 控制已登录 ChatGPT 标签页 |

所有端口都应只绑定回环地址。需要从电脑调试手机时，应使用 ADB 端口转发，而不是开放公网监听。

## 会话与路由

- 每个群会话使用稳定的 `session_id`，由 AstrBot 通过 OpenAI 请求的 `user` 字段传给桥。
- 默认模型、Opus 和 GPT 会话使用不同的派生键，避免不同模型共享错误上下文。
- 后台群记忆压缩会话带有 `group-memory-compressor:` 前缀，路由器会强制它留在默认模型。
- Antigravity 会话默认最多复用 20 轮、空闲 6 小时，并限制同时存在的持久化 TUI 数量。
- ChatGPT 网页会话默认最多复用 10 轮或 180000 字符；超过阈值后主动开启新对话，防止页面 DOM 与上下文无限增长。

## PTY 稳定性

本节只适用于没有配置 `ANTIGRAVITY_MANAGER_BASE_URL` 的兼容模式。Manager 模式不创建 PTY，也不依赖 `agy` 工作目录或本地 OAuth 状态。

持久化 Antigravity 桥不把“本轮回答结束”当作停止读取终端的信号。专属工作线程持续排空 TUI 输出并写入滚动屏幕缓冲，避免全屏刷新塞满 PTY。输入采用有界等待；提交后检查界面变化；生成期间检查进度；取消时关闭会话并等待工作线程退出。单个会话由一个工作线程独占，请求通过队列串行进入，避免重试线程同时写同一终端。

手机当前发布默认值：160×50 终端、5 秒写入超时、20 秒提交检测、180 秒无进度超时、256 屏滚动缓冲、最多 2 个持久化会话。

## 群聊记忆

群消息与结构化摘要存储在 AstrBot 数据目录下的 SQLite 数据库中。默认配置如下：

- 最近原文：18 条；
- 摘要批次：30 条；
- 注入摘要：最近 3 批；
- 安静窗口：90 秒；
- 摘要结构：主题、事实与决定、参与者立场、未解决问题、资源、检索关键词。

压缩任务异步启动并使用低优先级会话标识。模型不确定历史信息时，可调用本地历史搜索工具，从原文与摘要中按关键词、用户或时间范围检索，而不是凭印象补全。

## ChatGPT 网页桥

网页桥直接使用 Chrome DevTools Protocol，不保存或导出浏览器登录资料。它把多轮消息拼成增量输入，用粘贴事件分块写入编辑器，观察停止按钮与回答变化，检测错误卡片并做有界重试。文本和图片结果都会转换为 OpenAI 风格响应；图片下载到 proot 可见的临时目录，并按保留时间清理。

网页 DOM、选择器和服务端行为随时可能变化，因此该组件必须被视为可降级的实验路由，而不是系统唯一模型入口。

## 运行监控

`deployment/termux/stack-monitor.mjs` 检查 AstrBot、路由桥、ChatGPT 桥和 CDP 健康状态，并读取各层日志中的阶段时间。`napcat-watchdog.sh` 通过 AstrBot 的 QQ 消息活动、NapCat 进程和终端离线标记区分“群里安静”“进程退出”和“QQ 被踢下线”。自动重启默认关闭；启用前必须在手机本地设置 `NAPCAT_UIN`。
